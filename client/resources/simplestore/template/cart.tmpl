# Current Cart

Last Updated: {{.Updated.Format "2006-01-02 15:04:05" }}

{{template "cart-listing.tmpl" .}}

**Total: {{ money .Total }}**{{ with .TotalDCR }} · ≈ {{.}}{{ end }}

{{if .HasUnavailable}}
Something in your cart is no longer sold. Remove it and you can order the
rest.
{{end}}

---
## Place Order
{{- $shipping := false -}}
{{- range .Items -}}
  {{- if .Product.Shipping -}}
      {{- $shipping = true -}}
      {{- break -}}
  {{- end -}}
{{- end -}}

{{- if .HasUnavailable}}
{{else}}
--form--
type="action" value="/placeOrder"
{{- if $shipping}}
type="txtinput" label="Name" name="name"
type="txtinput" label="Address" name="address1"
type="txtinput" label="Address (optional)" name="address2"
type="txtinput" label="City" name="city"
type="txtinput" label="State" name="state"
type="txtinput" label="PostalCode" name="postalCode"
type="txtinput" label="Phone" name="phone"
{{- end}}
{{- if .Methods.Both}}
type="select" label="How would you like to pay?" name="method" options="ln|Lightning — settles straight away, onchain|On-chain — settles in a block or two"
{{- end}}
type="submit" label="Place Order" style="primary" confirm="Place this order for {{ money .Total }}{{ with .TotalDCR }} (about {{.}}){{ end }}? The shop is told what you have ordered and gives you a way to pay."
--/form--
{{end}}

--form[align=right]--
type="action" value="/clearCart"
type="submit" label="Clear Cart" style="danger" confirm="Take everything out of your cart? Nothing is ordered and there is no undo."
--/form--
