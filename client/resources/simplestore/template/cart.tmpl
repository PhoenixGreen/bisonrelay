{{ steps "Shopping Cart" "cart" }}

Last Updated: {{.Updated.Format "2006-01-02 15:04:05" }}

{{template "cart-listing.tmpl" .}}

**Total: {{ money .Total }}**{{ with .TotalDCR }} · ≈ {{.}}{{ end }}

{{if .HasUnavailable}}
Something in your cart is no longer sold. Remove it and you can order the
rest.
{{end}}

---

{{- /*
One button out of the cart, and it does not place anything.

The cart used to ask for an address, a way to pay and the order itself in one
press, and everything wrong with any of it was found out afterwards. Those
questions are the checkout's now: see checkout.go.
*/ -}}

{{if .HasUnavailable}}
{{else}}
--form--
type="action" value="/checkout"
type="submit" label="Proceed to checkout" style="primary"
--/form--
{{end}}

--form[align=right]--
type="action" value="/clearCart"
type="submit" label="Clear Cart" style="danger" confirm="Take everything out of your cart? Nothing is ordered and there is no undo."
--/form--
