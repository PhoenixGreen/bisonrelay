# Order #{{.ID.Num}} placed

--panel[padding=16, margin=0 0 16 0, border=1, color=accent, radius=8]--
{{if .PayURI -}}
## Pay {{ money .Total }} — {{.TotalDCR}}

{{ .PayURI }}

{{if eq .PayType "onchain" -}}
{{ with .PayQR }}--qr[size=200]--
{{.}}
--/qr--
{{ end }}
Scan that with a wallet, or send exactly {{.TotalDCR}} to the address above.
The shop watches for it and marks your order as soon as the network confirms
it — you do not have to stay on this page, and the payment goes through
whether or not this app is running.
{{- else -}}
That is a Lightning invoice: paying it settles straight away.
{{- end}}

{{ with .ExpiresIn }}The amount holds for another {{.}}, after which the rate
is worked out again.{{ end }}
{{- else -}}
## Order placed

The seller will be in touch with how to pay.
{{- end}}
--/panel--

--panel[padding=16, border=1, color=outline, radius=8]--
{{template "cart-listing-plain.tmpl" .Cart}}

Items: {{ money .Cart.Total }}
{{ if gt .ShipCharge 0.0 }}Shipping: {{ money .ShipCharge }}
{{ end }}Total: {{ money .Total }}

At {{.ExchangeRate}} DCR/$ — {{.TotalDCR}}
--/panel--

[Your orders](/orders) · [Back to the shop]({{storeIndex}})
