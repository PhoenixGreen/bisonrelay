# Order #{{.ID}} placed

{{if eq .PayType "ln" -}}
**Pay {{.TotalDCR}}** with Lightning:

lnpay://{{.Invoice}}
{{- else if eq .PayType "onchain" -}}
**Send {{.TotalDCR}}** to:

{{ .Invoice }}
{{- end}}

This amount holds for the next 60 minutes, after which the rate is worked
out again.

--panel[padding=16, border=1, color=outline, radius=8]--
{{template "cart-listing.tmpl" .Cart}}

Items: {{ money .Cart.Total }}
Shipping: {{ money .ShipCharge }}
Total: {{ money .Total }}

At {{.ExchangeRate}} DCR/$ — {{.TotalDCR}}
--/panel--
